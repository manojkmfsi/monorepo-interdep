# NPM Registry Configuration
registry=https://registry.npmjs.org/

# Automatically authenticate with token from environment or .npmrc
# Token should be provided via NPM_TOKEN environment variable in CI/CD

# Allow publishing scoped packages
@monorepo:registry=https://registry.npmjs.org/
